{
  "name": "listflow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names SERVER,CLIENT --prefix-colors cyan,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
